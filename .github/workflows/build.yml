name: build

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write # needed to create releases on v* tags

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache vcpkg binary archives
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\vcpkg\archives
          key: vcpkg-${{ hashFiles('vcpkg.json') }}
          restore-keys: vcpkg-

      - name: Set up vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg "$env:GITHUB_WORKSPACE\vcpkg"
          & "$env:GITHUB_WORKSPACE\vcpkg\bootstrap-vcpkg.bat"

      - name: Configure
        env:
          VCPKG_ROOT: ${{ github.workspace }}\vcpkg
        run: cmake --preset default

      - name: Build
        run: cmake --build --preset release

      - name: Test
        run: ctest --test-dir build -C Release --output-on-failure

      - name: Package
        run: |
          New-Item -ItemType Directory package | Out-Null
          Copy-Item build/Release/LogicalSystem.exe package/
          Copy-Item build/Release/*.dll package/
          Copy-Item README.md, LICENSE package/
          Compress-Archive -Path package/* -DestinationPath LogicalSystem-win64.zip

      - uses: actions/upload-artifact@v4
        with:
          name: LogicalSystem-win64
          path: LogicalSystem-win64.zip

  macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache vcpkg binary archives
        uses: actions/cache@v4
        with:
          path: ~/.cache/vcpkg/archives
          key: vcpkg-macos-${{ runner.arch }}-${{ hashFiles('vcpkg.json') }}
          restore-keys: vcpkg-macos-${{ runner.arch }}-

      - name: Set up vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg "$GITHUB_WORKSPACE/vcpkg"
          "$GITHUB_WORKSPACE/vcpkg/bootstrap-vcpkg.sh"

      - name: Configure
        env:
          VCPKG_ROOT: ${{ github.workspace }}/vcpkg
        run: cmake --preset macos

      - name: Build
        run: cmake --build --preset macos-release

      - name: Test
        run: ctest --test-dir build-macos --output-on-failure

      - name: Package
        run: ditto -c -k --sequesterRsrc --keepParent build-macos/LogicalSystem.app LogicalSystem-macos-${{ runner.arch }}.zip

      - uses: actions/upload-artifact@v4
        with:
          name: LogicalSystem-macos-${{ runner.arch }}
          path: LogicalSystem-macos-${{ runner.arch }}.zip

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [windows, macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/**/*.zip
